# Repo Health Radar Report

Score: **87/100** (B)

## High Impact Fixes

- **Release readiness: Git tags are present** - Create version tags for published releases.
- **Security and trust: Code scanning signal exists** - Add CodeQL, Semgrep, or another code scanning workflow for higher-trust projects.

Use `repo-health-radar --path . --format markdown` to generate a current report for your repository.
